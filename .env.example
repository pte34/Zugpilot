# Optional: Supabase-Sync. Ohne diese beiden Werte laeuft ZugPilot rein
# lokal ueber localStorage (siehe README.md, Abschnitt "Optional: Supabase-Sync").
#
# Kopiere diese Datei nach ".env.local" und trage deine eigenen Werte ein
# (Supabase-Dashboard -> Project Settings -> API).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
